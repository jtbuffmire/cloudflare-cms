# =============================================================================
# Cloudflare CMS - Environment Template
# =============================================================================
# Copy this file to .env (development) or .env.production (production)
# and fill in your values.
#
# NEVER commit .env or .env.production to git!
# =============================================================================

# -----------------------------------------------------------------------------
# CLOUDFLARE AUTHENTICATION
# -----------------------------------------------------------------------------
# API token with Workers, D1, R2, and Pages permissions
# Create at: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your_api_token_here

# -----------------------------------------------------------------------------
# WORKER SECRETS
# These are synced to Cloudflare secrets during deployment
# -----------------------------------------------------------------------------

# Environment mode
ENVIRONMENT=development

# JWT secret for authentication (generate with: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here

# D1 Database ID (from Cloudflare dashboard)
DATABASE_ID=your_database_id_here

# Session duration in seconds (default: 86400 = 24 hours)
SESSION_DURATION=86400

# Admin credentials - comma-separated lists (order must match!)
# Example: user1@example.com,user2@example.com
ADMIN_EMAIL=admin@localhost
# Example: password1,password2
ADMIN_PASSWORD=your_password_here

# CORS allowed origins - comma-separated list of allowed domains
# Include both main domain and admin subdomain for each site
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4174,http://localhost:8787

# -----------------------------------------------------------------------------
# FRONTEND VARIABLES (Vite)
# These are used by the blog and admin SvelteKit apps
# -----------------------------------------------------------------------------

# API endpoint URL
VITE_API_URL=http://localhost:8787

# API version prefix
VITE_API_VSN=/api/v1

# Environment mode for frontend
VITE_ENVIRONMENT=development

# WebSocket URL for real-time updates
VITE_WS_URL=ws://localhost:8787
