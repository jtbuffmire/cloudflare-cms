# =============================================================================
# Cloudflare CMS - Environment Variables
# =============================================================================
# This is the single source of truth for all environment variables.
#
# Setup:
#   1. Copy this file to .env for local development:
#      cp .env.template .env
#
#   2. For production, copy to .env.production:
#      cp .env.template .env.production
#
# The dev.sh and deploy.sh scripts will automatically load and distribute
# these variables to the appropriate services.
# =============================================================================

# -----------------------------------------------------------------------------
# CLOUDFLARE AUTHENTICATION
# -----------------------------------------------------------------------------
# API token for Cloudflare deployments (wrangler)
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Workers Scripts Edit, D1 Edit, R2 Edit, Pages Edit
CLOUDFLARE_API_TOKEN=

# -----------------------------------------------------------------------------
# WORKER VARIABLES
# -----------------------------------------------------------------------------
# Environment mode: development | production
ENVIRONMENT=development

# JWT secret for authentication tokens
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Admin credentials (comma-separated for multiple users)
# Each email at index N corresponds to password at index N
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=

# D1 Database ID
# Find with: npx wrangler d1 list
DATABASE_ID=

# Session duration in seconds (default: 24 hours)
SESSION_DURATION=86400

# CORS allowed origins (comma-separated)
# Development: http://localhost:5173,http://localhost:4174
# Production: https://yourdomain.com,https://admin.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4174

# -----------------------------------------------------------------------------
# FRONTEND VARIABLES (Vite)
# -----------------------------------------------------------------------------
# These are prefixed with VITE_ to be exposed to the browser

# API base URL
# Development: http://localhost:8787
# Production: https://api.yourdomain.com
VITE_API_URL=http://localhost:8787

# API version prefix
VITE_API_VSN=/api/v1

# Environment name for frontend
VITE_ENVIRONMENT=development

# WebSocket URL (derived from API URL, but can be overridden)
# Development: ws://localhost:8787
# Production: wss://api.yourdomain.com
VITE_WS_URL=ws://localhost:8787
