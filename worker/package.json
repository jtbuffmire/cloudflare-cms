{
  "name": "worker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "node migrations/migrate.js",
    "migrate:prod": "node migrations/migrate.js production",
    "db:reset": "npx wrangler d1 execute cms-db --local --file=./migrations/sql/0000_initial.sql",
    "db:reset_hard": "node scripts/setup.js",
    "db:nuke": "echo 'Please delete the database manually from Cloudflare dashboard at https://dash.cloudflare.com/'",
    "test-token": "node scripts/generate-test-token.js",
    "db:meals": "npx wrangler d1 execute cms-db --local --file=./migrations/sql/0000_initial.sql && npx wrangler d1 execute cms-db --local --file=./migrations/sql/0001_meals_starter.sql",
    "dev": "wrangler dev",
    "build": "wrangler deploy",
    "deploy": "wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/jsonwebtoken": "^9.0.7",
    "dotenv": "^16.4.7",
    "jsonwebtoken": "^9.0.2",
    "sass": "^1.83.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^3.1.3",
    "marked": "^15.0.5",
    "xss": "^1.0.15"
  }
}
